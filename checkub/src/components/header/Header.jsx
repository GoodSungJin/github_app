import React from 'react';
import { HeaderConainer } from './Header-styled';

const Header = () => {
  return (
    <HeaderConainer className="contain">
      이거슨 헤더 컨테이너다
    </HeaderConainer>
  )
};

export default Header;